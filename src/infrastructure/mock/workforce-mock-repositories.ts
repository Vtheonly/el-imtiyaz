/**
 * Mock workforce repository implementations — iteration 8 (plan §09 expansion).
 *
 * ---------------------------------------------------------------------------
 * REFACTOR NOTE (iteration 2):
 * The implementation has been moved to `./workforce/index.ts`. This file is
 * now a thin re-export shim so existing imports from
 * `@/infrastructure/mock/workforce-mock-repositories` continue to work.
 * ---------------------------------------------------------------------------
 */
export * from "./workforce";
