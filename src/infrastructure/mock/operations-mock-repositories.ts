/**
 * Mock operations repository implementations — iteration 9.
 *
 * ---------------------------------------------------------------------------
 * REFACTOR NOTE (iteration 2):
 * The implementation has been moved to `./operations/index.ts`. This file is
 * now a thin re-export shim so existing imports from
 * `@/infrastructure/mock/operations-mock-repositories` continue to work.
 * ---------------------------------------------------------------------------
 */
export * from "./operations";
