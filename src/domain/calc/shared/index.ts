/**
 * Shared calculation utilities — money + date math.
 *
 * Re-exports the contents of `money.ts` and `dates.ts` so callers can
 * import everything from `@domain/calc/shared` (or `@domain/calc`).
 */
export * from "./money";
export * from "./dates";
